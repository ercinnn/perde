/// Fixed reference "today" used across the demo so seeded mock data
/// (orders, weekly plan, stock) stays internally consistent regardless of
/// the device's real calendar date.
final DateTime mockToday = DateTime(2026, 7, 14);
